# flask-youtube-podcasts
## setup:
python -m venv env

activate.bat (windows)  //  source env/bin/activate (linux)

pip install -r requirements.txt

set FLASK_APP=app.py (windows)  // export FLASK_APP=app.py (linux)

flask run
